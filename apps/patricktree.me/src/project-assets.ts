import createAudiobookFromURLImage from "#pkg/writing/projects/cup-create-an-audiobook-from-a-url.png";

export const PROJECT_ASSETS = {
  CREATE_AUDIOBOOK_FROM_URL: {
    image: createAudiobookFromURLImage,
    imageAlt:
      "Cup - Create an Audiobook from a URL interface with a URL field and an Upload & listen button.",
  },
} as const;
