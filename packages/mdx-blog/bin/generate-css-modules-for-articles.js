/* this shim is the package's bin entrypoint, its whole job is to load the build output */
// oxlint-disable-next-line no-restricted-imports
import "../dist/bin/generate-css-modules-for-articles.js";
